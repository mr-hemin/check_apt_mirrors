# This bash script checks available mirrors from a given list of apt mirrors.


# How to use:
##  After downloading the file, give execution permission:
```
sudo chmod +x check_mirrors.sh

```

## Run the script in the directory:
```
sudo ./check_mirrors.sh
```

## Then run the update repository command:
```
sudo apt update
```